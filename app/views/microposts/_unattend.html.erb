<%= form_for(current_user.events_attending.find_by(attendee_id: current_user.id),
             html: { method: :delete }) do |f| %>
   <div><%= hidden_field_tag :attending_id, micropostId %></div>
  <%= f.submit "Unattend", class: "btn" %>
<% end %>