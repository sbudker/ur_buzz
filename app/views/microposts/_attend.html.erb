<%= form_for(current_user.events_attending.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :attending_id, @micropost.id %></div>
  <%= f.submit "Attend", class: "btn btn-primary" %>
<% end %>